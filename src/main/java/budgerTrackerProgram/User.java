package budgerTrackerProgram;

/*User class which could be expanded upon in (a theoretical) further development of the program
But which is a record class for this program which only has one user (hard coded into the Main class (BudgetTracker)*/
public record User(long userID, String firstName, String lastName) {
}